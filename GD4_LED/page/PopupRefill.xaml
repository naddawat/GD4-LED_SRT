<Page x:Class="GD4_LED.page.PopupRefill"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Refill Medicine" 
      WindowTitle="Refill Medicine"
      Background="Transparent"
      ShowsNavigationUI="False">

    <Page.Resources>
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#212529"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                  Focusable="False"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#ADB5BD"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#007BFF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="8" Opacity="0.2" ShadowDepth="0" Color="#007BFF"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="ModernDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#212529"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Button Styles -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="#6C757D"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8F9FA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E9ECEF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5A6268"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#545B62"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#218838"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E7E34"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <!-- Main Popup Overlay covering entire available space -->
    <Grid x:Name="RefillPopupOverlay" 
          Background="#80000000"
          Panel.ZIndex="999"
          Margin="0"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">

        <!-- Popup Card Container -->
        <Border Background="White"
                CornerRadius="16"
                Width="656"
                Height="643"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center" Loaded="Border_Loaded">

            <!-- Drop Shadow Effect -->
            <Border.Effect>
                <DropShadowEffect BlurRadius="32" 
                                  Opacity="0.25" 
                                  ShadowDepth="8"
                                  Color="Black"/>
            </Border.Effect>

            <!-- Main Content -->
            <Grid Margin="32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header Section -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="เติมยา" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   Foreground="#212529"/>
                        <TextBlock Text="เพิ่มจำนวนยาในสต็อก" 
                                   FontSize="16" 
                                   Foreground="#6C757D" 
                                   Margin="0,8,0,0"/>
                    </StackPanel>

                    <Button Grid.Column="1" 
                            Content="✕" 
                            Style="{StaticResource CloseButtonStyle}"
                            Click="CancelRefill_Click"/>
                </Grid>

                <!-- Drug Information Card -->
                <Border Grid.Row="2"
                        CornerRadius="12" 
                        Padding="24">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#F8F9FA" Offset="0"/>
                            <GradientStop Color="#E9ECEF" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock x:Name="DrugCodeText" 
                                       FontSize="18" 
                                       FontWeight="Bold" 
                                       Foreground="#212529"/>
                            <TextBlock x:Name="DrugNameText" 
                                       FontSize="16" 
                                       Foreground="#495057" 
                                       Margin="0,4,0,8"/>
                            <TextBlock x:Name="LocationText" 
                                       FontSize="14" 
                                       Foreground="#6C757D"/>
                        </StackPanel>

                        <Border Grid.Column="1" 
                                Background="White"
                                CornerRadius="12"
                                Padding="20"
                                MinWidth="100">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="คงเหลือปัจจุบัน" 
                                           FontSize="12" 
                                           Foreground="#6C757D" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock x:Name="QuantityText" 
                                           FontSize="36" 
                                           FontWeight="Bold" 
                                           Foreground="#007BFF" 
                                           HorizontalAlignment="Center"
                                           Margin="0,4,0,4"/>
                                <TextBlock x:Name="UnitText" 
                                           Text="เม็ด" 
                                           FontSize="14" 
                                           Foreground="#6C757D" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <!-- Input Form -->
                <StackPanel Grid.Row="4">
                    <!-- จำนวนที่เติม -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="จำนวนที่ต้องการเติม *" 
                                       FontSize="15" 
                                       FontWeight="SemiBold" 
                                       Foreground="#495057" 
                                       Margin="0,0,0,8"/>
                        <TextBox  
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Text=""
                                     IsEnabled="True"
                                     IsReadOnly="False"
                                     Focusable="True"
                                     TabIndex="1"/>
                    </StackPanel>

                    <!-- Lot Number และ Expiry Date -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Lot Number *" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           Foreground="#495057" 
                                           Margin="0,0,0,8"/>
                            <TextBox x:Name="RefillLotTextBox" 
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         Text=""/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="วันหมดอายุ *" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           Foreground="#495057" 
                                           Margin="0,0,0,8"/>
                            <DatePicker x:Name="RefillExpiryDatePicker" 
                                            Style="{StaticResource ModernDatePickerStyle}"/>
                        </StackPanel>
                    </Grid>

                    <!-- หมายเหตุ -->
                    <StackPanel/>
                </StackPanel>

                <!-- Action Buttons -->
                <Grid Grid.Row="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" 
                            Content="ยกเลิก" 
                            Style="{StaticResource CancelButtonStyle}"
                            Click="CancelRefill_Click"/>

                    <Button Grid.Column="2" 
                            Content="ยืนยันเติมยา" 
                            Style="{StaticResource SuccessButtonStyle}"
                            Click="ConfirmRefill_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>